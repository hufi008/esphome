# speaker_4.yaml
substitutions:
  device_name: "esphome-web-d47dbc"
  friendly_name: "Smart Speaker 3"
  media_player_entity_id: "media_player.${ friendly_name | lower | replace(' ', '_') }_audio_player"

logger:
  level: INFO

packages:
  # Holt die Grundfunktionen (WLAN, API, etc.)
  base: !include common/base-s3_devkitc_1.yaml
  # Holt die Funktionalität
  hardware: !include voice_satellite/hardware.yaml
  functions: !include voice_satellite/main.yaml
  scripts: !include voice_satellite/scripts.yaml
  climate: !include common/bme280.yaml
  # climate: !include common/htu21.yaml
  sun: !include common/sunset_rise.yaml
  moon: !include common/moonphase.yaml
  weather: !include common/weather.yaml
